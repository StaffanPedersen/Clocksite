# Clocksite

## Project Setup

npm run dev-prod
was intended for dev enviroment setup making it use local server and local api. Production api is cors protected and will not work with this setup.

npm run format
Makes prettier shine on the code.
